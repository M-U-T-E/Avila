# Shared

This directory contains code shared between the web client, server, and desktop client. This includes data types, API definitions, and other common utilities.

## Structure

*   `src/`: Contains the shared source code.
*   `package.json`: Lists the project's dependencies and scripts.
*   `tsconfig.json`: TypeScript configuration file.

